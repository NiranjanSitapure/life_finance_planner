[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[phases.release]
cmds = ["npx prisma migrate deploy"]

[start]
cmd = "node dist/index.js"
