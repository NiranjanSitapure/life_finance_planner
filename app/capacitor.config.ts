import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.niranjansitapure.lifefinanceplanner',
  appName: 'Life Finance Planner',
  webDir: 'dist'
};

export default config;
